const ASSET_BASE='https://raw.githubusercontent.com/DigitalDoha/digital-doha-site/main/three-adventure/assets/images/';document.querySelectorAll('img[src^="./assets/images/"]').forEach(img=>{img.src=ASSET_BASE+img.getAttribute('src').split('/').pop()});document.querySelectorAll('source[srcset^="./assets/images/"]').forEach(source=>{source.srcset=ASSET_BASE+source.getAttribute('srcset').split('/').pop()});const menu=document.getElementById('menu');const links=document.getElementById('links');if(menu&&links){menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');menu.setAttribute('aria-expanded','false')}))}document.querySelectorAll('.media-slot img').forEach(img=>{const slot=img.closest('.media-slot');const loaded=()=>{if(img.naturalWidth>0){slot?.classList.add('has-image');img.hidden=false}};const failed=()=>{img.hidden=true;slot?.classList.remove('has-image')};img.addEventListener('load',loaded);img.addEventListener('error',failed);if(img.complete){img.naturalWidth>0?loaded():failed()}});document.querySelectorAll('.js-logo').forEach(img=>{let triedFallback=false;const fallback=()=>{const src=img.dataset.fallback;if(!triedFallback&&src){triedFallback=true;img.hidden=false;img.src=src}else{img.hidden=true}};img.addEventListener('error',fallback);if(img.complete&&img.naturalWidth===0)fallback()});const reveal=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('on');reveal.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>reveal.observe(el));const typing=document.getElementById('typing');const phrases=['Carnival games for every age','Colorful event activations','Family entertainment in Doha'];let p=0,c=0,back=false;function type(){if(!typing)return;const text=phrases[p];typing.textContent=text.slice(0,c);if(!back&&c<text.length){c++;setTimeout(type,65)}else if(!back){back=true;setTimeout(type,1400)}else if(c>0){c--;setTimeout(type,35)}else{back=false;p=(p+1)%phrases.length;setTimeout(type,300)}}type();const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();
